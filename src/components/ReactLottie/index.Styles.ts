import { createStyles } from "@mui/styles";

const Styles = () =>
    createStyles({
        container: {
            height: "100%",
            width: "100%",
            overflow: "hidden",
            margin: "0 auto",
            outline: "none",
        },
    });

export default Styles;
