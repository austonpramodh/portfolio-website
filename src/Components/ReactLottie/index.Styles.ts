import { createStyles } from "@material-ui/styles";
// import { Theme } from "@material-ui/core";

const Styles = () =>
    createStyles({
        container: {
            height: "100%",
            width: "100%",
            overflow: "hidden",
            margin: "0 auto",
            outline: "none",
        },
    });

export default Styles;
