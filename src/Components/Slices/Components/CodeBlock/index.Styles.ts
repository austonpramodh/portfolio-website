import { createStyles } from "@material-ui/styles";
// import { Theme } from "@material-ui/core";

const Styles = () =>
    createStyles({
        container: {
            marginLeft: "auto",
            marginRight: "auto",
            maxWidth: 800,
        },
    });

export default Styles;
